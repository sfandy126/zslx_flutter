- Dart 第三方依赖统一在根 `pubspec.yaml` 的 dependencies/dev_dependencies 中声明，子模块不重复引入。
- 静态分析与 Lint 规则集中在根 `analysis_options.yaml`，供所有子模块共享。
- 资源文件按 `assets/images/`、`assets/images/tabbar/` 等目录组织，并通过 flutter_gen 生成类型安全的访问代码。