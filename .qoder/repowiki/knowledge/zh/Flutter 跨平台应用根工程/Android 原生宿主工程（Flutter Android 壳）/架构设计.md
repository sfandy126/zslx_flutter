该模块是标准的 Flutter Android 宿主工程，仅包含最少的原生代码以启动 Flutter 引擎：`app/src/main/kotlin/com/example/zslx_flutter/MainActivity.kt` 继承 `FlutterActivity` 作为唯一 Activity 入口；`GeneratedPluginRegistrant.java` 由 Flutter 工具自动生成，用于注册插件。构建体系基于 Gradle Kotlin DSL：根 `build.gradle.kts` 统一仓库源并将所有子项目构建产物输出到上层 `../../build` 目录，避免与 Flutter 构建目录冲突；`settings.gradle.kts` 通过 `local.properties` 定位 Flutter SDK，并引入 `flutter-plugin-loader`、`com.android.application:9.0.1` 与 `org.jetbrains.kotlin.android:2.3.20` 插件；`app/build.gradle.kts` 声明 `compileSdk/targetSdk/minSdk=24`、Java/Kotlin 目标为 JVM 17，并通过 `flutter { source = "../.." }` 将源码指向项目根目录的 Dart/Flutter 代码。资源位于 `res/` 下，按 Android 标准组织 drawable、mipmap、values 等。依赖方向单向：原生层仅负责启动 Flutter 引擎，业务逻辑全部在 Flutter 侧实现。