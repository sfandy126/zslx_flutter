- 原生入口保持极简：`MainActivity` 直接继承 `FlutterActivity`，不添加额外逻辑，业务 UI 完全由 Flutter 侧管理。
- 构建脚本统一使用 Kotlin DSL（`.kts`），并在根 `build.gradle.kts` 中将所有子项目构建目录重定向到上层 `../../build`，避免与 Flutter 自身构建目录冲突。
- 版本与 SDK 信息集中声明：`compileSdk`、`targetSdk`、`ndkVersion` 等通过 `flutter.*` 扩展属性读取，确保与 Flutter 工具链一致。