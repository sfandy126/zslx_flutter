- 每个页面文件对应一个独立 Widget 类，命名遵循 `<业务名>Page` 并以 PascalCase 表示。
- 展示型页面统一继承 `StatelessWidget`，并在构造函数中使用 `const` 与 `super.key` 声明。
- 页面主体采用 `Scaffold` + `AppBar`（标题居中）+ `Center` 包裹的 `Column` 布局结构。
- 底部导航项通过数据模型 `_TabItem` 集中配置 label、normalIcon、selectedIcon，再映射为 `BottomNavigationBarItem`。